# create-naxi-extension

A small util that will scaffold a naxi extension.

## Installation

This package is meant to be used through `npm` or `yarn`:

```
npx create-naxi-extension
```

```
yarn create naxi-extension
```
